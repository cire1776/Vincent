describe('Vincent integration test suite', () => {
  it('runs a basic test to verify Jest and TypeScript setup', () => {
    expect(1 + 1).toBe(2);
  });

  // Example: add more integration tests here as you build features
});
